# UI Problems To-Do

This checklist is based on `obsidian/sssas/prompts.md`. Each item will be marked complete as the issue is fully solved and verified.

- [x] Admin panel create-user form should expand with a smooth slide-down animation inside the card.
- [x] Admin panel navbar should match the home page navbar design language, spacing, alignment, and crest glow.
- [x] Page transition timing should be fixed so the next page is revealed only after the transition closes and reopens smoothly.
- [x] Admin panel sections should have stronger spacing, floating-card treatment, and section titles moved outside the cards.
- [x] Admin panel dropdown styling should match the site UI.
- [x] System logs should show online users and login history, and the admin panel should not show the bottom-right notification bell.
- [x] Coach panel should match the shared design language, use navbar menu navigation, become a scrollable one-page layout, and keep only player health data.
- [x] Tactical board should allow removing a player from a filled position back into the player list.
- [x] Coach/manager/player success alerts should use a top alert style without shaking the layout.
- [x] Manager panel should match the shared design language and expand forms inside cards so they are fully visible.
- [x] Create and document a reusable UI design language so future changes stay visually consistent across pages.
- [x] Player panel/request area should match the shared design language, and the New Request form should use a slide-down reveal animation.
